using UnityEngine;

public class BounceSurface : MonoBehaviour
{
    // =========================
    // Custom bounce sound
    // =========================
    public AudioClip bounceSFX;
    // instead of the default bounce sound when colliding with this surface.
}